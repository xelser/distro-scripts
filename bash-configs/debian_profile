
# Post-Install Script
if [ -xf $HOME/debian-final.sh ]; then
	sh -c "$HOME/debian-final.sh"
fi

# Startup Commands
echo $XDG_SESSION_TYPE | grep -q x11
if [ $? -eq 0 ]; then
	numlockx on
	xset led 3
fi
